{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(cpp) Launch",
            "type": "cppdbg",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "program": "${workspaceFolder}/build/tarot.exe",
            "preLaunchTask": "Build with CMake"
        }
    ]
}